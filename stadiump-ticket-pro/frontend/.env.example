# Frontend — copier en .env.local pour le dev
# En production (Vercel), définir ces variables dans le dashboard Vercel.

# URL de l’API backend (obligatoire en production)
VITE_API_URL=http://localhost:8000

# Clé publique Stripe (optionnel, pour le formulaire de paiement)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxx
